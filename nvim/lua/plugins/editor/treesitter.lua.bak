return {
  "nvim-treesitter/nvim-treesitter",
  opts = {
    ensure_installed = {
      "prisma", -- Add Prisma support (if available in your Treesitter setup)
      -- Other languages
    },
    highlight = {
      enable = true,
      additional_vim_regex_highlighting = false,
    },
  },
}
